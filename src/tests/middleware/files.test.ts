import filesController from '../../controllers/files.controller';

describe('saveFile', () => {
  it('should successfully save a file to the database', async () => {});
});

describe('downloadFile', () => {
  it('should successfully download a file from the database', async () => {});
});

describe('markFileUnsafe', () => {
  it('should successfully mark a file as unsafe', async () => {});
});

describe('deleteFile', () => {
  it('should successfully delete a file from the database', async () => {});
});

describe('streamMedia', () => {
  it('should successfully stream media', async () => {});
});
